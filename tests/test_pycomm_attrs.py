
def main(comm):
	assert hasattr(comm, "messages")
	assert hasattr(comm, "active_game")
	assert hasattr(comm, "new_game")
	assert hasattr(comm, "save_game")
	assert hasattr(comm, "resign_game")
	assert hasattr(comm, "make_move")
	assert hasattr(comm, "board_fen")
	assert hasattr(comm, "is_creating_new_game")
	assert hasattr(comm, "set_player_type")
	assert hasattr(comm, "cancel_new_game")
	assert hasattr(comm, "start_new_game")
	assert hasattr(comm, "load_saved_game")





